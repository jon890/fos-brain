# fos-skills 공용 스킬 코어 진화 (2026-07-30 분석)

`~/personal/fos-skills` — planning·build-with-teams·review-fix·docs-check·adjust-harness 공용 코어 스킬 저장소의 git 커밋 로그(80개) 전체를 분석해 뽑은 요약. 세션 정제 없이 `git log --format="%H%n%s%n%b"` 를 직접 읽었다.

## 반복되는 리팩터 패턴

- "다른 곳이 소유한 지시를 걷어낸다" 류 커밋이 8개 이상 — 같은 지시가 핵심 원칙 절과 절차 절 양쪽에 있으면 절차 절(더 구체적인 쪽)을 소유자로 남기고 상위 요약에서 지운다.
- 정렬 비교로 "내용은 그대로고 위치만 바뀌었는지" 확인하는 절차가 반복된다.
- 끊긴 참조(다른 스킬 소유의 파일 경로를 자기 것처럼 잘못 적은 경우)가 검사 도구를 만들자마자 여러 번 발견됐다.

## 버저닝 체계 (`5491ae0`)

SKILL.md frontmatter에 version, 스킬별 CHANGELOG.md, 올리는 기준 3등급(minor/major/patch). 심링크 소비 방식이라 버전은 배포 핀이 아니고, 태그·루트 CHANGELOG를 두지 않는다. evaluation(측정 도구)은 버전 대신 기준값 파일 날짜로 추적.

## 스킬 채점 도구 (`936207a`, `1c1aee7`)

`score.sh`(기계축: 분량, 끊긴 참조, 표기 위반), `rubric.md`(판단축: 0~3점, 작성자 자기채점 금지). 슬림화 직전 기준값을 기록해 변경 전후를 비교했다.

## 실행 기록 도입 (`a642982`)

파이프라인 실행마다 critic REVISE·code-reviewer FIX_NEEDED·검증 판정·사용자 개입 횟수가 발생하지만 세션이 끝나면 사라진다. 실행 하나를 한 줄로 항상 기록(중단도 포함), 회고는 사건 하나를 깊게 없으면 안 쓴다. 핵심 지표는 개입 횟수.

## subagent 위임 원칙 (`a8e93e4`, `5c11047`)

Opus 계열은 "보수적으로"·"중대한 것만" 지시를 문자 그대로 따라 실제 결함 보고까지 줄인다. code-reviewer·critic·docs-check 위임 모두 판정과 발견 목록을 분리해 전부 보고받고, 필터링·최종 분류는 위임한 쪽(team-lead)이 한다.

## build-with-teams 신규 결정 (여러 커밋)

- 모델 등급 표: parent 등급을 명시하지 않으면 자식이 다른 등급으로 뜬다(`bfa38de`).
- 스폰 폴백 단계적 하강: team_name+name → name만 → 이름 없는 subagent 순, 폴백 사유를 보고에 남긴다(`e5ed8e1`).
- planning→build 단일 브랜치·단일 PR 전환(G1): plan 브랜치에 구현이 이어 붙어 기획+구현 커밋이 한 PR에 담긴다(`2a2af78`).
- phase 단위 executor teardown 강제 — 팀원 누적 방지(`aa19d32`).
- docs-verifier 2회 → 1회 통일, code-reviewer 전부 보고, team-lead 필터(`5c11047`).

## planning 신규 결정 (여러 커밋)

- 통과 조건을 "사용자 확정 대기"에서 "모호점 0"으로 전환 — 막아야 할 것은 모호함을 안고 앞서가는 것이지 단계를 연달아 통과하는 게 아니다(`ad99f10`, `5289b69`).
- 필수 문서 5종 계약에 규모별 완화 추가 — 책임 문서를 안 건드리는 변경은 판정 한 줄로 갈음(`1a596a6`).
- 선행 plan 전제를 phase 본문에 명시(의존 필드는 여전히 금지)(`92e6b67`).

## docs-check 신규 결정

- inline 셸 레시피를 `scripts/static-check.sh`로 분리, 마크다운 문법 4종(코드펜스 홀수·표 열 불일치·헤딩 레벨 건너뜀·깨진 상대링크) 자동 검출 추가(`12cd01e`, `f5aefce`).
- 한국어 표기 검사를 공용 검사기(`~/.claude/scripts/korean-style-check.sh`)에 위임 — 규칙 문서에만 있던 항목(금지어 41회·`+` 87회 누적)이 실행되는 검사를 붙이자 더는 쌓이지 않았다(`71566376`, `9e9dd50`).

## 제외한 것

verify-task.sh 상대경로 cwd 함정, git grep 대체, 번호 시퀀스 오탈자 교정 등 좁은 기술 버그 수정은 durable 가치가 낮아 후보에서 뺐다 — brain의 4축(업무 스타일·취향·기술 의사결정·AI 에이전트 스킬 구조) 기준에 맞지 않는다.
