engine-strict=true

# pnpm 은 .tool-versions 를 읽지 않으므로 pnpm 이 스크립트를 돌릴 node 를 직접 핀한다 (.tool-versions 와 동일 버전)
use-node-version=22.16.0

# Quartz 는 npm flat node_modules(hoisting) 전제로 일부 transitive 의존성을 직접 import 한다. pnpm strict 에서 깨지므로 hoisted 링커 사용.
node-linker=hoisted
