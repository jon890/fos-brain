# 비공개 네임스페이스 — 절대 commit 하지 않는다
private/
.claude/worktrees/
work/

# Quartz 빌드 산출물·의존성
quartz/node_modules/
quartz/public/
quartz/.generated/
quartz/.quartz-cache/
quartz/quartz/.quartz-cache/
quartz/tsconfig.tsbuildinfo

# 로컬 전체 빌드 — content(심볼릭 병합)·output 은 무시, serve.sh 스크립트는 추적
quartz-local/content/
quartz-local/public/

# 로컬 에이전트 상태 (OMC)
.omc/

# brain 플러그인 — 세션 포인터(개인 경로·session id 포함, 공개 저장소에 커밋 금지)
.agents/plugin/fos-brain/staging/*.jsonl

# 잡파일
.DS_Store
